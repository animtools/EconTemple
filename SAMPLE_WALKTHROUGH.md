# サンプルで学ぶ EconTemple 基本操作

同梱の `sample_files/` を使い、[QuickStart.md](./QuickStart.md) の **ステップ1〜5（基本ワークフロー）** を一通り体験するための手順書です。スクリーンショットや詳細な説明は QuickStart を参照してください。

**想定時間**: 約15〜20分（初回起動・パネル調整の程度により変動）

---

## このドキュメントと QuickStart の違い

| 項目 | 本書（SAMPLE_WALKTHROUGH） | QuickStart.md |
|------|---------------------------|---------------|
| 目的 | 同梱サンプルだけで基本操作を完了する | 機能全体の概要と詳細手順 |
| 前提 | ZIP 内の `sample_files/` をそのまま使う | 自分のコンテフォルダでも可 |
| 分量 | 最短経路に絞る | 全機能・用語・トラブルシュート |

---

## 同梱サンプルの場所

配布 ZIP（例: `EconTemple-v0.9.0-beta-win64.zip`）を解凍すると、おおよそ次の構成になります。

```text
（解凍したフォルダ）/
├── EconTemple/          … アプリ本体（EconTemple.exe）
└── sample_files/        … 本書で使うサンプル一式
    ├── サンプルプロジェクト.eco
    ├── コンテサンプル/
    │   ├── sample_conte/     … ページ画像 6 枚（page_016.jpg 〜 page_021.jpg）
    │   └── sample_conte.pdf  … PDF 読み込み体験用（任意）
    ├── 素材サンプル/         … レイアウト用紙・CLIP テンプレート（書き出しの応用）
    └── サンプル書き出しフォルダ/ … 書き出し先の例（空でも可）
```

**重要**: `サンプルプロジェクト.eco` は、同じ `sample_files/` 内の `コンテサンプル/sample_conte` を **相対パス**で参照しています。`sample_files/` 以下のフォルダ構成を崩さないでください（`.eco` だけ別フォルダに移すと画像が読めません）。

---

## 事前準備

1. ZIP を解凍する。
2. `EconTemple/EconTemple.exe` を起動する（初回は Windows Defender の「詳細情報」→「実行」が必要な場合があります）。
3. エクスプローラーで `sample_files/` の場所を確認しておく。

---

## 手順0: サンプルプロジェクトを開く（QuickStart ステップ1・2 のショートカット）

新規作成とコンテフォルダ指定を済ませた状態のプロジェクトが同梱されています。

1. メニュー **「ファイル」** → **「プロジェクトを開く(&O)...」**
2. `sample_files/サンプルプロジェクト.eco` を選択して開く。
3. 中央の **「ページブラウザ」** に **6 ページ**（`page_016.jpg` 〜 `page_021.jpg`）が並ぶことを確認する。
4. いずれかのページをクリックし、右側の **グラフィックス領域** にコンテ画像が表示されることを確認する。

**含まれているもの / 含まれていないもの**

- 含まれる: コンテグループ（略称 `smp`）、6 ページ分の画像パス
- 含まれない: カット（0 件）、ページごとの確定したパネル矩形（開いたあとに設定します）

> **別ルート（参考）**: ゼロから試す場合は [付録A](#付録a-コンテフォルダだけから新規に読み込む) を参照。

---

## 手順1: 画面の見方を確認する（QuickStart ステップ2）

| 領域 | タブ名（UI の正） | このサンプルでの役割 |
|------|-------------------|----------------------|
| 左 | 設定タブ（「表示」「レイアウト用紙」「ページ」「パネル」「カット」） | パネル・用紙の調整 |
| 中央上 | **ページブラウザ** | 6 ページの切り替え |
| 中央下 | **カットブラウザ** | 手順2でカット一覧を表示 |
| 右 | グラフィックス領域 | 赤枠＝抽出領域、青枠＝100F（フレーム） |
| 中央 | **書き出し** | 手順3で画像・XDTS を出力 |

ページを切り替えながら、グラフィックス上にパネル枠が出るか確認します。枠が無い場合は次の手順2へ進んでください。

---

## 手順2: パネル設定（QuickStart ステップ3・要点のみ）

サンプル `.eco` にはパネル矩形が保存されていません。QuickStart ステップ3 と同様に、**バッチパネル設定**で大まかに決めてから、必要なら個別調整します。

### 2-1. レイアウト用紙（任意・推奨）

1. 左の設定タブ **「レイアウト用紙」** を開く。
2. 実運用に近いサイズを入力するか、**「レイアウト用紙画像」** の **「参照」** で  
   `sample_files/素材サンプル/コンテ用紙.jpg` を読み込む（サンプル用の用紙画像）。
3. フレームサイズ・余白を確認する（詳細は QuickStart「ステップ3: パネル設定」）。

### 2-2. バッチパネル設定（必須）

1. 設定タブ **「パネル」** → **「バッチパネル設定」** を開く。
2. **パネル数**・**抽出領域**（上端・左端・横幅・高さ）を、見本コンテに合わせて設定する。  
   各ページのコマ数が同じなら、1 ページ目を見ながら数値を入れると早いです。
3. グラフィックス領域で **赤い抽出枠** と **青いフレーム枠** が全ページに出るか、ページブラウザで切り替えて確認する。

### 2-3. 個別調整（必要なときだけ）

- 枠の位置を直す: グラフィックス上でパネルを選択し、ドラッグまたはハンドルで調整（QuickStart「3-4. 個別パネルの調整」）。
- フレームだけ直す: グラフィックス上部の **フレーム編集モード** をオンにして調整。

---

## 手順3: カットを作成する（QuickStart ステップ4・要点のみ）

パネルが揃ったら、カットに紐づけます。サンプルは **カット 0 件** のため、まず自動作成します。

1. **「カットブラウザ」** タブを開く（初期状態では「カットがありません」と表示されます）。
2. ヘッダー左の **更新アイコン**（ツールチップ: **「現在のグループのカット番号再計算を実行（カットが存在しない場合は自動作成）」**）をクリックする。  
   → 各パネルに対応するカットが自動作成され、一覧に表示されます。
3. カット番号の並びを整えたいときは、同じ **更新アイコン** を再度実行する（再計算）。

**注意（削除アイコンとの違い）**

- **削除スイープアイコン**（ツールチップ: **「現在のグループの全カットをクリアして自動再作成」**）は、既存のカットデータ（尺・セリフなど）も消します。初回の自動作成には **更新アイコン** を使ってください。

手動でパネルをまとめたい・カットデータを入れたい場合は、QuickStart「ステップ4: カット作成」を参照してください。

---

## 手順4: 書き出す（QuickStart ステップ5・要点のみ）

1. **「書き出し」** タブを開く。
2. **出力フォルダ** で保存先を指定する（例: `sample_files/サンプル書き出しフォルダ` またはデスクトップ上の空フォルダ）。
3. 初回テスト向けのおすすめ設定:
   - **書き出し単位**: 「カット」または「全て」
   - **XDTSファイルを書き出す**: オン（クリップスタジオ連携を試す場合）
   - **書き出し後にフォルダを開く**: オン（結果をすぐ確認）
4. **「カットデータ書き出し」** ボタンをクリックする（処理中は同ボタンがキャンセル表示に切り替わります）。
5. 完了後、出力フォルダにカットフォルダ・パネル画像・（設定時）XDTS などが生成されていることを確認する。

**CLIP テンプレートを試す場合（任意）**

- **書き出し** タブの **テンプレートフォルダ** に  
  `sample_files/素材サンプル/カットフォルダ` を指定すると、書き出し時に `{cut_identifier}_*.clip` などのテンプレートを各カットフォルダへコピーできます（ファイル名のプレースホルダは実際のカット識別子に置き換わります）。

出力フォルダの構造の詳細は QuickStart「ステップ5: 書き出し」「5-3. 出力結果の確認」を参照してください。

---

## うまくいかないとき

| 症状 | 確認すること |
|------|----------------|
| ページはあるが画像が真っ白 | `.eco` と `コンテサンプル/` が同じ `sample_files/` 内にあるか。フォルダを分けて移動していないか。 |
| パネル枠が出ない | 「パネル」タブの **バッチパネル設定**（パネル数・抽出領域）を設定したか。 |
| カット一覧が空のまま | **更新アイコン**（カット番号再計算）を押したか。パネルが 1 つも無い状態ではカットは作られません。 |
| 書き出しボタンが押せない | 出力フォルダが指定されているか。カットが作成されているか。 |

その他は [QuickStart.md](./QuickStart.md) のトラブルシュート、[USER_GUIDE.md](./USER_GUIDE.md)、[SUPPORT.md](./SUPPORT.md) を参照してください。

---

## 次に読むもの

- [QuickStart.md](./QuickStart.md) … PDF 読み込み、ページ除外、シーン・担当者、兼用カットなど
- [USER_GUIDE.md](./USER_GUIDE.md) … 操作の詳細・困ったとき
- [CHANGELOG.md](./CHANGELOG.md) … バージョンごとの変更

---

## 付録A: コンテフォルダだけから新規に読み込む

同梱 `.eco` を使わず、コンテフォルダ指定の練習をする場合の最短手順です。

1. **「ファイル」** → **「新規プロジェクト」**（未保存のプロジェクトが開きます）。
2. 画面上部のコンテグループ欄で **➕**（ツールチップ: **「新しいConteGroupを追加」**）をクリック。
3. **「コンテグループ作成」** ダイアログで:
   - **略語** などを入力（例: `smp`）
   - **ルートフォルダ** に、プロジェクトを `sample_files/` に保存する前提なら  
     `コンテサンプル/sample_conte` のように **プロジェクトファイルからの相対パス** を指定  
     （またはフォルダ参照で `sample_files/コンテサンプル/sample_conte` を選択）
4. **「作成」** をクリック → 6 ページがページブラウザに読み込まれます。
5. 以降は本書の **手順2（パネル）** 以降と同じです。

**ヒント**: 相対パスは、`.eco` を保存するフォルダ（通常は `sample_files/`）と `コンテサンプル/` の位置関係が変わらない限り、別 PC へフォルダごとコピーしても有効です。

---

## 付録B: 同梱ファイル一覧（開発・メンテ用）

| パス | 用途 |
|------|------|
| `サンプルプロジェクト.eco` | 6 ページ読み込み済みの開始点 |
| `コンテサンプル/sample_conte/*.jpg` | コンテページ画像 |
| `コンテサンプル/sample_conte.pdf` | PDF 変換フローの試用（QuickStart ステップ2） |
| `素材サンプル/コンテ用紙.jpg` | レイアウト用紙の参照画像 |
| `素材サンプル/LO用紙.jpg` | LO 用紙サンプル |
| `素材サンプル/カットフォルダ/*.clip` | 書き出しテンプレート |
| `サンプル書き出しフォルダ/` | 書き出し先の例（空フォルダ可） |

リポジトリ内の更新元は `release-repo/_dev/sample_files/` です。配布 ZIP へはビルド時に `sample_files/` として同梱されます。

---

**EconTemple 開発チーム**
