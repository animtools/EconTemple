# サンプルで学ぶ EconTemple 基本操作

同梱の `sample_files/` を使い、EconTemple の基本ワークフローを体験するための手順書です。スクリーンショットや詳細な説明は [QuickStart.md](./QuickStart.md) を参照してください。

---

## 進め方（2 ルート）

| ルート | 目的 | 想定時間 |
|--------|------|----------|
| **[ルートA](#ルートa-完成サンプルを開いて確認する)** | 完成プロジェクトを開き、画面の見方とデータの入り方を確認する | 約 5〜10 分 |
| **[ルートB](#ルートb-サンプル素材から完成状態まで組み立てる)** | 同梱コンテから、QuickStart と同じ手順で完成状態まで作る | 約 15〜25 分 |

初めて触る場合は **ルートA → ルートB** の順がおすすめです。時間がない場合はルートAだけでも構いません。

---

## 同梱サンプルの場所

配布 ZIP（例: `EconTemple-v0.9.0-beta-win64.zip`）を解凍すると、おおよそ次の構成になります。

```text
（解凍したフォルダ）/
├── EconTemple/          … アプリ本体（EconTemple.exe）
└── sample_files/        … 本書で使うサンプル一式
    ├── サンプルプロジェクト.eco   … 完成例
    ├── コンテサンプル/
    │   ├── sample_conte/     … コンテ用ページ画像
    │   └── sample_conte.pdf  … PDF 読み込み体験用
    ├── 素材サンプル/         … レイアウト用紙・CLIP テンプレート
    └── サンプル書き出しフォルダ/ … 書き出し先の例
```

**重要**: `サンプルプロジェクト.eco` は、同じ `sample_files/` 内の `コンテサンプル/sample_conte` を **相対パス**で参照しています。`sample_files/` 以下のフォルダ構成を崩さないでください（`.eco` だけ別フォルダに移すと画像が読めずパスの再設定が必要になります）。

---

## 事前準備

1. ZIP を解凍する。
2. `EconTemple/EconTemple.exe` を起動する（初回は Windows Defender の「詳細情報」→「実行」が必要な場合があります）。
3. エクスプローラーで `sample_files/` の場所を確認しておく。

---

## ルートA: 完成サンプルを開いて確認する

完成例がどのような状態かを、サンプルプロジェクトを開いて確認できます。

### A-1. プロジェクトを開く

1. メニュー **「ファイル」** → **「プロジェクトを開く(&O)...」**
2. `sample_files/サンプルプロジェクト.eco` を選択して開く。
3. 左サイドバーの **「ページブラウザ」** に、コンテのページが並ぶことを確認する。
4. いずれかのページをクリックし、右側の **グラフィックス領域** にコンテ画像と **パネル枠** が表示されることを確認する。

（画面イメージ: QuickStart「[サンプル](./QuickStart.md#サンプル)」のスクリーンショット参照）

### A-2. パネル設定が済んでいること

| 確認する場所 | 見るポイント |
|--------------|--------------|
| グラフィックス領域 | 各ページのコンテにパネル枠が付いている|
| 設定タブ **「パネル」** | **「一括設定:」** の数値が入っている。 |

ページを切り替え、枠の位置がページごとにコンテの絵に合わせてパネルが設定されていることを確認できます。詳細な調整手順は [QuickStart ステップ3](./QuickStart.md#ステップ3-パネル設定) を参照してください。

### A-3. カット割り当てが済んでいること

1. 左サイドバーの **「カットブラウザ」** を開く。
2. **カット一覧** にカットのデータが並んでいることを確認する。
3. 行を選び、グラフィックス上で対応するパネル枠が強調されることを確認する。

カットの作り方・番号の付け方は [QuickStart 4-1・4-2](./QuickStart.md#4-1-カット作成) を参照してください。

### A-4. 尺・カットデータが入っていること

1. カットブラウザで任意のカットを 1 つ選択する。
2. 設定パネルの **「カット」** タブを開く。
3. **尺**（秒＋コマ）や **パネル一覧**、必要に応じて **Dialogue** / **Camera** タブに値が入っていることを確認する。

入力の詳細などは [QuickStart 4-3](./QuickStart.md#4-3-カットデータの編集) を参照してください。

### A-5. シーン・担当者が割り当て済みであること

| 確認する場所 | 見るポイント |
|--------------|--------------|
| カットブラウザの **シーン** 列 | シーン名（例: `A` / `B` など）が入っている行がある |
| カットブラウザの **担当者** 列 | 担当者名が入っている行がある |

割り当て手順は [QuickStart 4-4](./QuickStart.md#4-4-カットのグループを編集)（シーン分け・担当者割当て）を参照してください。**兼用カット** 列も [QuickStart の兼用カット](./QuickStart.md#兼用カット) で確認できます。

### A-6. 書き出しを試す（任意）

完成データのまま出力を体験する場合:

1. **「書き出し」** タブを開く。
2. **出力フォルダ** に `sample_files/サンプル書き出しフォルダ` などのフォルダを指定する。
3. **「カットデータ書き出し」** を実行し、カットフォルダ・パネル画像・（設定時）XDTS ができることを確認する。

手順の詳細は [QuickStart ステップ5](./QuickStart.md#ステップ5-書き出し) です。CLIP テンプレートは `sample_files/素材サンプル/カットフォルダ` を **テンプレートフォルダ** に指定すると試せます。

---

## ルートB: サンプル素材から完成状態まで組み立てる

ルートA で見た **完成例と同じ素材** を使い、ゼロに近い状態から同じゴールまで進めます。各段階の詳細・画像は QuickStart の対応節を開いてください。

**開始のしかた（どちらか）**

- **おすすめ**: 下記 **[B-1](#b-1-プロジェクトとコンテquickstart-ステップ12)** のとおり **新規プロジェクト** から `コンテサンプル/sample_conte` を読み込む（パネル・カットはこれから設定）。

### 工程一覧（QuickStart 対応表）

| 順番 | 本書でのゴール | QuickStart（主な参照先） |
|------|----------------|-------------------------|
| B-1 | プロジェクト＋コンテ読み込み | [ステップ1](./QuickStart.md#ステップ1-プロジェクト作成)・[ステップ2](./QuickStart.md#ステップ2-コンテ画像の読み込み)（本書 B-1 の手順） |
| B-2 | レイアウト用紙・パネル枠 | [ステップ3](./QuickStart.md#ステップ3-パネル設定)（[3-2](./QuickStart.md#3-2-レイアウト用紙設定) 任意で `素材サンプル/コンテ用紙.jpg`、[3-3](./QuickStart.md#3-3-一括設定全ページで共通の設定) 必須、[3-4](./QuickStart.md#3-4-個別パネルの調整) は必要時のみ） |
| B-3 | カット作成・番号 | [4-1](./QuickStart.md#4-1-カット作成)（カットブラウザの **削除スイープ** でパネルから自動作成）、[4-2](./QuickStart.md#4-2-カット番号一括計算)（必要時） |
| B-4 | 尺・セリフ・カメラ等 | [4-3](./QuickStart.md#4-3-カットデータの編集)（サンプル `.eco` の値を見ながら真似して入力してもよい） |
| B-5 | シーン・担当者 | [4-4](./QuickStart.md#4-4-カットのグループを編集)（シーン管理の有効化→割当て、担当者管理） |
| B-6 | 書き出し | [ステップ5](./QuickStart.md#ステップ5-書き出し) |

### B-1. プロジェクトとコンテ（QuickStart ステップ1・2）

完成例 `.eco` を使わず、コンテフォルダだけから読み込む手順です（QuickStart のステップ1・2に相当）。

1. **「ファイル」** → **「新規プロジェクト」**（未保存のプロジェクトが開きます）。
2. 画面上部のコンテグループ欄で **➕**（ツールチップ: **「新しいConteGroupを追加」**）をクリック。
3. **「コンテグループ作成」** ダイアログで:
   - **略語** などを入力（例: `smp`）
   - **ルートフォルダ** に、プロジェクトを `sample_files/` に保存する前提なら  
     `コンテサンプル/sample_conte` のように **プロジェクトファイルからの相対パス** を指定  
     （またはフォルダ参照で `sample_files/コンテサンプル/sample_conte` を選択）
4. **「作成」** をクリック → フォルダ内のすべてのページがページブラウザに読み込まれます。

**ゴール**: ページブラウザに、選択したコンテフォルダ内のページが並ぶ。

**ヒント**: 相対パスは、`.eco` を保存するフォルダ（通常は `sample_files/`）と `コンテサンプル/` の位置関係が変わらない限り、別 PC へフォルダごとコピーしても有効です。画面の詳細は [QuickStart ステップ1・2](./QuickStart.md#ステップ2-コンテ画像の読み込み) を参照してください。

### B-2. パネル設定（QuickStart ステップ3）

1. **「レイアウト用紙」** で `sample_files/素材サンプル/LO用紙.jpg` を参照。
2. **「パネル」** タブの **一括設定** でパネル数・抽出領域を設定する。
3. 不要パネルの削除・足りないパネルの追加・パネルの個別調整 [3-4 個別調整](./QuickStart.md#3-4-個別パネルの調整) をする。

**ゴール**: 各ページのパネルがコンテ画像に合っている状態。

### B-3. カット作成（QuickStart ステップ4・4-1〜4-2）

1. **「カットブラウザ」** を開く。
2. ツールバー左の **リセットボタン** で、パネルに対応するカットを一括作成する。
3. 番号の並びを直す必要があれば **カット番号** メニューで再番号付けする。

**注意**: すでに尺・シーン等を入力したあとでリセットをすると、確認のうえデータが消える場合があります。

**ゴール**: カットブラウザにカットが並ぶ（ルートA と同程度の件数でなくても、必要なパネルにカットが付いていれば可）。

### B-4. カットデータ（QuickStart 4-3）

1. カットを選び **「カット」** タブで **尺** を入力する。
2. 時間があれば Dialogue / Camera タブも試す。

**ゴール**: 主要カットに尺が入っている。サンプル完全再現は必須ではありません。

### B-5. シーン・担当者（QuickStart 4-4）

1. カットブラウザで **シーン管理を有効化** し、シーン列に割り当てる。
2. **担当者管理** で担当者列に割り当てる。

**ゴール**: シーン列・担当者列に値が入る。サンプルと同じ名前でなくても構いません。

### B-6. 書き出し（QuickStart ステップ5）

[ルートA の A-6](#a-6-書き出しを試す任意) と同様に、出力フォルダを指定して **カットデータ書き出し** を実行します。

### ルートB のチェックリスト

- [ ] B-1: コンテのページがページブラウザに読み込み済み
- [ ] B-2: パネル枠が全ページで確認できる
- [ ] B-3: カットブラウザにカットがある
- [ ] B-4: 少なくとも 1 カットに尺を入力した
- [ ] B-5: シーン・担当者を 1 件以上割り当てた
- [ ] B-6（任意）: 書き出しが成功した

ルートA の完成例と見比べ、足りない部分があれば QuickStart の該当節だけ戻って補足してください。

---

## 次に読むもの

- [QuickStart.md](./QuickStart.md) … PDF 読み込み、ページ除外、兼用カット、クリップスタジオ連携など
- [SUPPORT.md](./SUPPORT.md) … 質問・不具合報告・要望（テスト段階の問い合わせ先）
- [CHANGELOG.md](./CHANGELOG.md) … バージョンごとの変更

---

## 付録: 同梱ファイル一覧（開発・メンテ用）

| パス | 用途 |
|------|------|
| `サンプルプロジェクト.eco` | **完成例**（パネル・カット・尺・シーン・担当者まで設定済み。ルートA の入口） |
| `コンテサンプル/sample_conte/*.jpg` | コンテページ画像（ルートB でも使用） |
| `コンテサンプル/sample_conte.pdf` | PDF 変換フローの試用（QuickStart ステップ2） |
| `素材サンプル/コンテ用紙.jpg` | レイアウト用紙の参照画像 |
| `素材サンプル/LO用紙.jpg` | LO 用紙サンプル |
| `素材サンプル/カットフォルダ/*.clip` | 書き出しテンプレート |
| `サンプル書き出しフォルダ/` | 書き出し先の例（空フォルダ可） |

リポジトリ内の更新元は `release-repo/_dev/sample_files/` です。配布 ZIP へはビルド時に `sample_files/` として同梱されます。

---

**EconTemple 開発チーム**
