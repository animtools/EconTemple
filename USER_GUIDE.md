# EconTemple - ユーザーガイド

このドキュメントは、EconTemple の **操作の詳細** と **つまずきやすいポイントの対処** をまとめたものです。
最短の使い方は [QUICKSTART](./QUICKSTART.md) を参照してください。

---

## 目次

- [はじめに（全体の流れ）](#はじめに全体の流れ)
- [用語（このガイド内の呼び方）](#用語このガイド内の呼び方)
- [画面構成（どこで何をするか）](#画面構成どこで何をするか)
- [基本操作（つまずかない手順）](#基本操作つまずかない手順)
- [プロジェクト（作成・保存・復元）](#プロジェクト作成保存復元)
- [コンテの読み込み（PDF/画像・グループ）](#コンテの読み込みpdf画像グループ)
- [パネル抽出（自動抽出と調整）](#パネル抽出自動抽出と調整)
- [カット管理（割り当て・タイムライン・尺）](#カット管理割り当てタイムライン尺)
- [兼用カット（複数カットでパネルを共有）](#兼用カット複数カットでパネルを共有)
- [ページ描画（作打ちメモ）](#ページ描画作打ちメモ)
- [ガイド（プリセット・フォーカス/ルックルーム）](#ガイドプリセットフォーカスルックルーム)
- [表示（オーバーレイ・表示モード）](#表示オーバーレイ表示モード)
- [書き出し（出力フォルダ・単位・XDTS・テンプレート）](#書き出し出力フォルダ単位xdtsテンプレート)
- [ショートカット（確認・編集・初期化）](#ショートカット確認編集初期化)
- [メニュー別リファレンス](#メニュー別リファレンス)
- [よくあるつまずき（原因 → 確認 → 解決）](#よくあるつまずき原因--確認--解決)
- [困ったとき（報告・問い合わせ）](#困ったとき報告問い合わせ)

## はじめに（全体の流れ）

EconTemple の基本的な流れは次の通りです。

1. **プロジェクトを作成/開く**
2. **コンテを読み込む**（PDF または画像）
3. **パネル抽出・調整**（自動検出＋必要なら手動調整）
4. **カット管理**（パネルをカットに割り当て、順序/尺/情報を管理）
5. **書き出し**（パネル画像、XDTS、必要に応じて付随データ）

---

## 用語（このガイド内の呼び方）

- **ページ**: 読み込んだコンテの各ページ（画像/PDFのページ）
- **パネル**: ページから切り出す「コマ」単位の矩形
- **カット**: パネルに割り当てる作業単位（番号/尺などを管理）
- **グループ**: 複数ページをまとめて扱う単位（プロジェクト内のまとまり）
- **コンテフォルダ**: コンテ画像（ページ画像）が入ったフォルダ（グループに登録して参照します）
- **アクティブなコンテフォルダ**: グループ内でいま参照しているコンテフォルダ
- **出力フォルダ**: 書き出し先フォルダ（存在するフォルダが必須）

---

## 画面構成（どこで何をするか）

EconTemple は、主に次の領域で操作します（名称は UI 表記に合わせます）。

- **グループ選択（左上）**: コンテグループの選択・追加・編集・削除
- **ページブラウザ（タブ: ページブラウザ）**: ページ一覧、除外ページ、ブックマーク
- **カットブラウザ（タブ: カットブラウザ）**: カット一覧（テーブル）、複数選択
- **書き出し（タブ: 書き出し）**: 出力フォルダ、書き出し単位、対象、XDTS/テンプレート等
- **設定タブ（右側）**:
  - **表示**: 表示・オーバーレイの設定
  - **レイアウト用紙**: レイアウト/余白など
  - **ページ**: 整列（整列設定/確定/キャンセル等）
  - **パネル**: 抽出パラメータ、矩形/フレーム調整、パネル画像アクション
  - **カット**: カットのプロパティ、パネル、セリフ、カメラ、ガイド、兼用カット
- **グラフィックス（中央）＋ツールバー（上部）**:
  - **描画:** 描画オフ/パネル描画/ページ描画
  - **表示:** 見開き/カット/パネル（表示モード）

このガイドでは、まず作業フロー順に説明し、最後にメニュー別の参照表を置きます。

---

## 基本操作（つまずかない手順）

### 1) プロジェクトを作成/開く

- メニュー **「ファイル(&F)」** から **「新規プロジェクト(&N)」** または **「プロジェクトを開く(&O)...」** を選びます。
- 作業の保存は **「保存(&S)」** / **「名前を付けて保存(&A)...」** を使います。

### 2) コンテを読み込む

コンテは基本的に、**コンテグループにコンテフォルダ（画像フォルダ）を登録**して読み込みます。

1. 左上でグループを選択（なければ追加）
2. グループ編集を開く
3. **「コンテフォルダを管理...」** から、画像フォルダを追加（必要なら PDF を読み込んで追加）
4. **「アクティブなコンテフォルダ」** を選んで参照先を切り替える

### 3) パネル抽出・調整

- 自動検出されたパネル（矩形）を確認します。
- 必要に応じて、矩形を調整して抽出結果を整えます。
- ページごとに条件が違う場合は、ページ単位で調整してから進めます。

### 4) カット管理（割り当て・移動）

- パネルをカットに割り当て、順序や尺を管理します。
- **カット番号で移動**: メニュー **「編集(&E)」→「カット番号で移動(&J)...」** を使うと、番号を指定して移動できます。

### 5) 書き出し（XDTS を含む）

書き出しに失敗する/ボタンが押せない場合、まず以下を確認してください。

- **出力フォルダは必須**です（未指定だと実行できません）
- **指定した出力フォルダが存在する必要**があります（存在しないパスだと実行できません）
- 書き出し対象（ページ/カット等）が必要です（対象が0だと実行できません）

---

## プロジェクト（作成・保存・復元）

- **新規プロジェクト**: メニュー **「ファイル(&F)」→「新規プロジェクト(&N)」**
- **開く**: メニュー **「ファイル(&F)」→「プロジェクトを開く(&O)...」**
- **保存/名前を付けて保存**: メニュー **「ファイル(&F)」→「保存(&S)」/「名前を付けて保存(&A)...」**

ポイント:

- **プロジェクトは作業状態を保存・復元**するためのファイルです（ページ、パネル、カット、各種設定を含みます）。
- プロジェクトの拡張子は `.cpe` です。
- 未保存の変更がある状態で **新規プロジェクト作成** / **プロジェクトを開く** を行うと、次の確認が出ます。
  - 「現在のプロジェクトに未保存の変更があります。\n新しいプロジェクトを作成する前に保存しますか？」
  - 「現在のプロジェクトに未保存の変更があります。\nプロジェクトを読み込む前に保存しますか？」
- 新規プロジェクト作成後は、初期セットアップとして **コンテグループ作成**のダイアログが開きます（キャンセルも可能）。

---

## コンテの読み込み（PDF/画像・グループ）

EconTemple の「コンテ読み込み」は、基本的に **コンテ画像が入ったフォルダ（コンテフォルダ）をグループに登録**して行います。  
（現状の実装では、PDF/画像ファイルを“直接開く”よりも、フォルダ登録が主ルートです。）

### 画像フォルダを登録して読み込む（基本）

グループには **コンテフォルダを複数登録（スロット）**でき、切り替えて参照できます。  
コンテフォルダは少なくとも1つ必要です（0にはできません）。

入口（UI）:

1. 画面左上の **グループ選択**で対象グループを選びます
2. 右側の **編集ボタン**（ツールチップ: 「現在のConteGroupを編集」）を押します
3. 「コンテグループ編集」ダイアログで **「コンテフォルダを管理...」** を押します

### PDF を読み込む（画像に変換してフォルダとして追加）

グループのコンテフォルダ管理には **「PDFを読み込んで追加」**があります。

- PDF を画像へ変換し、**新しいコンテフォルダとして追加**します
- 変換ダイアログでは次が必須です
  - **PDFファイル（必須）**
  - **出力フォルダ（必須）**（存在するフォルダ）
- 変換後は、出力フォルダの配下に **PDF名のフォルダ**が作成され、その中に `page_001.jpg` のような連番画像が生成されます

### フォルダ切り替え時の挙動（重要）

コンテフォルダ（スロット）の扱いにはモードがあります。

- **フォルダごとにページ・カットデータを別々に扱う**
  - **オフ（共有）**: フォルダを切り替えてもページ/カットの編集データは共通で、参照する画像だけが切り替わります
  - **オン（分離）**: フォルダごとにページ/カットの編集データが独立します（フォルダ削除時は、そのフォルダ専用の編集データも破棄されます）

### グループについて

- 読み込んだページ群は、プロジェクト内で **グループ**としてまとまります。
- グループを切り替えると、ページ一覧・カット一覧・書き出し対象もそのグループに対応して変わります。

---

## パネル抽出（自動抽出と調整）

基本:

- 自動抽出で検出されたパネル矩形を確認し、必要なら調整します。
- ページごとに枠線や余白条件が異なる場合、ページ単位で調整して進めます。

### ページ一覧（除外・ブックマーク）

- 左側に **「ブックマーク」** と **「ページ一覧」** があります。
  - ブックマーク済みページは「ブックマーク」へ表示され、**ダブルクリックで移動**できます
- **「除外されているページを表示」** を ON にすると、除外ページ一覧が表示されます
  - ON 時はボタン表記が **「除外されているページを非表示」** に変わります

### 一括設定（ページ共通の抽出パラメータ）

「一括設定:」は、グループ全体に効く基準値です（ページごとの例外がない場合はここを詰めるのが最短です）。

- **上**
- **左端**
- **高さ**
- **幅**
- **パネル数**
- **余白**

### パネル設定（選択パネルの調整）

「パネル設定:」は、選択中のパネル（矩形）に対して調整します。

- **ページのパネルを一括設定に合わせる**  
現在ページのパネルを、グループの一括（バッチ）設定に合わせてリセットします。

#### 抽出矩形の調整

- **矩形 上 / 左 / 高さ / 幅** を調整すると、グラフィックス上の矩形に反映されます。

#### フレーム矩形の編集（100Fr）

- **フレーム矩形を編集（100Fr）** を ON にすると、編集対象が抽出矩形からフレーム矩形へ切り替わります。  
  - ツールチップ: 「抽出矩形とフレーム矩形の編集を切り替えます」

フレーム（パネル内の複数フレーム）操作:

- フレーム一覧（ドラッグで並び替え可）
- **+**: フレームを追加
- **−**: 選択フレームを削除（最後の1つは削除不可）
- **プライマリに設定**: 選択フレームをプライマリフレームに設定する
- **名前を入れ替え**: 選択中の 2 フレームの名前トークンを入れ替える
- **名前方式**:  
  - 「英大文字 (A, B, ...)」/「整数 (1, 2, ...)」
  - 方式変更時は既存フレーム名が振り直されるため確認が出ます
- **回転(°)**: 選択中フレームの回転角度  
  - ツールチップ: 「選択中フレームの回転角度。 [ / ] でも±回転できます（Shiftで大きく回転）。」

### パネル画像（ビュー上の見た目を保存）

「パネル画像:」では、選択パネルの **ビュー上の見た目（ズーム・オーバーレイ込み）** を扱えます。

- **クリップボードにコピー**: 選択パネルのビュー上の画像をクリップボードへコピー
- **画像として書き出し**: 選択パネルのビュー上の画像をファイルとして書き出し

### 抽出（パネル切り出しを開始）

抽出ボタン（ツールチップ: **「パネル切り出しを開始」**）が有効になる条件:

- 入力フォルダ（コンテ）が選ばれている
- **出力フォルダが指定され、かつ存在するフォルダである**
- 書き出し対象（カット/ページ等）が選ばれている（書き出し単位により条件が変わります）

押せないときは、ボタンのツールチップが理由を教えてくれます（例: 「出力フォルダが存在しません。」「書き出し対象（カット・ページ・パネル）を追加してください。」）。

### パネルの削除・除外・マージ（グラフィックス）

パネル（抽出矩形）を選択した状態で、次の操作ができます。

- **追加（右ドラッグ）**: グラフィックス上で **右ボタンを押したままドラッグ**して範囲を作り、右ボタンを離すと、その範囲で新しいパネル（矩形）を追加します。
  - 小さすぎる矩形（幅/高さが小さい）は無視されます
  - 追加先のページは、作成した範囲の中心位置から判定されます（見開きでも可）
  - 追加したパネルはカットに自動割り当てされないため、必要なら `C`（カット作成）で割り当てます
- **削除**: 選択したパネルを削除（ショートカット: `Delete` / `X`）
- **マージ**: 選択した複数パネルをマージ（同一ページのみ、ショートカット: `M`）
- **カット作成**: 選択パネルからカットを作成（ショートカット: `C`）
- **カットから外す**: 選択パネルを所属カットから外す（ショートカット: `Shift+C`）
- **除外/復帰**（右クリック）:
  - **抽出対象から除外（… panels）**
  - **抽出対象に復帰（… panels）**

---

## カット管理（割り当て・タイムライン・尺）

基本:

- パネルをカットに割り当て、順序や尺（フレーム数）を管理します。
- **カット番号で移動**: メニュー **「編集(&E)」→「カット番号で移動(&J)...」** またはショートカット（後述）

### カットのプロパティ

カットを選択すると「カットのプロパティ」が編集できます。

- **カット番号:** 表示・整理用（例: 24、24A）
  - **ロック/アンロック**: ロック時は自動再番号付けの対象外
  - **このカット以降を再番号付け**（ツールチップ: 「このカット以降を、このカット番号から再番号付け（確認あり）」）
- **尺（秒＋コマ）:** `秒` と `コマ` で指定します（グループのフレームレートに基づいてフレーム数へ換算）
- **メモ:** カットのメモ
- **タグ:** 「タグ（カンマ区切り）...」

### パネル（カット内の並び・選択）

カットタブの **「パネル」** では、カット内のパネル一覧を扱います。

- パネルを選択すると、グラフィックス表示や「パネル個別タイミング設定」も追従します

### パネル個別タイミング設定（均等配分の上書き）

「パネル個別タイミング設定」では、選択パネルに個別の尺を設定できます。

- ツールチップ:  
「選択したパネルに個別のタイミングを設定できます。設定しない場合は均等配分されます。」
- **尺:** `秒` + `コマ`
- **設定**: 選択パネルに個別タイミングを設定（1コマ未満は不可）
- **削除**: 選択パネルの個別タイミングを削除
- **全削除**: 全パネルの個別タイミングを削除

### セリフ / カメラ / ガイド

カットタブには **「セリフ」**、**「カメラ」**、**「ガイド」** があり、カットごとの情報を入力できます。  
（ガイドの詳細は「ガイド」章を参照）

### カットテーブルの右クリック（便利操作）

カットブラウザのカット一覧（テーブル）は、**右クリック**でコンテキストメニューが開きます。代表的な項目:

- **🗑️ カットを削除**（複数選択対応）
- **🔢 カット番号**
  - **このカット以降を再番号付け**
  - **選択範囲を再番号付け（…カット）**
- **🎬 シーン管理** / **👤 担当者管理** / **🔢 優先度管理**
  - 有効化/無効化
  - 割当変更（複数選択対応）
  - 同じ属性のカットを選択
- **🔗 兼用カット**
  - **兼用カットに設定（…カット）**
  - **兼用カットグループから削除**

---

## 兼用カット（複数カットでパネルを共有）

この機能は、いわゆる「パネルリンク」という語で誤解されやすいため、本ガイドでは **兼用カット**の文脈で説明します。

### できること

- 複数のカットを **同じ兼用グループ**に入れることで、兼用カットとして扱えます。
- 書き出しの **カット選択**でも、兼用グループがまとまって表示されます（例: `1-3-5` のような連結表示）。

### 操作（カット側の「兼用カット管理」）

前提:

- 兼用カットの操作は、**現在選択中のカット**を起点に行います。

#### 1) 兼用グループにカットを追加する

1. 兼用カットにしたい「起点のカット」を選択します。
2. 「兼用カット管理 (Shared Cuts)」内の **「カット追加:」** に、追加したいカット名（例: `Cut 24` / `24A`）を入力します。
  - 入力欄はオートコンプリート対応です（候補を選べます）
3. **「追加」**（または Enter）で、兼用グループに追加します。

補足:

- 追加先のカットがすでに兼用グループに参加している場合、起点のカットをそのグループへ合流させる動きになります。
- **両方のカットがすでに別々の兼用グループに参加**している状態では追加できません。

#### 2) 兼用グループからカットを外す（解除）

1. 兼用グループに参加しているカットを選択します。
2. 「兼用グループのカット:」に表示される各カットの横の **「削除」** を押します。

挙動:

- 解除の結果、グループが **2カット未満**になる場合は、グループは自動的に解散します。

---

## ページ描画（作打ちメモ）

ページ上に描画・注記を残す機能です（外部説明では **ページ描画（作打ちメモ）** と表記します）。

### 重要な制約（必ず先に読む）

- **見開き表示でのみ有効**です（「見開き」表示でページ画像が表示されているとき）。  
利用できない場合は「見開き表示でページ画像が表示されているときのみ利用できます。」という警告が出ます。
- **ページ描画とパネル描画は同時に ON にできません**。
- **フレーム編集モード中は入れません**（ページ描画より優先されます）。

### 入り方 / 終わり方（ツールバー）

上部ツールバーの **「描画:」** で切り替えます。

- **「描画オフ」**
- **「パネル描画」**
- **「ページ描画」**（見開き表示でのみ有効）

ページ描画モードに入ると、右側に描画用パネルが表示されます。

### 描画対象ページの選び方

ページ描画モード中は、**描画したいページをクリック**して対象ページを切り替えます（見開き表示上でヒットしたページが対象になります）。

### クリア（削除）

右側の描画パネルから **クリア**を実行できます。  
クリア時は確認ダイアログが出て、文言は次の通りです。

- 「このページの描画をクリアしますか？（Undoで戻せます）」

### 表示・不透明度（表示タブ）

ページ描画そのものの表示は、表示設定から切り替えできます。

- 表示 → 表示・オーバーレイ → **「ページ描画: 表示する」**
- 不透明度 → **「ページ描画」** スライダー  
  - **ページ描画モード中は常に100%表示**になります（見えやすさ優先）

---

## ガイド（プリセット・フォーカス/ルックルーム）

ガイドは、カット（必要ならフレーム）に対して、構図用の基準線を表示する機能です。

### 共通設定

- **線幅:** ガイド線の太さを調整します。
- **フレームごとにガイドを分ける**: ON にすると、各フレームが独立したガイド設定を持ちます。  
  - ON のとき、「フレーム」一覧が表示され、編集対象フレームを選べます（グラフィックス上のフレーム選択と同期します）

### ガイド種別（チェックで有効化）

「ガイド」一覧は、**チェック＝有効/無効**、**選択＝編集対象**です。主な種別（UI 表示）は次の通りです。

- **三分割法**
- **センター**
- **対角線**
- **黄金比**
- **4x4**
- **アイライン**
- **ルックルーム**
- **ヘッドルーム**
- **セーフエリア**
- **フォーカルポイント**

### プリセット

ガイド上部のプリセット欄で操作します。

- **ガイドプリセット**: プリセットを選択
- **選択プリセットを適用**
- **現在設定をユーザープリセット保存**
- **選択ユーザープリセットを複製**
- **選択ユーザープリセットを削除**

### コピー/貼り付け（カット間で設定を移す）

- **現在カットのガイド設定をコピー**
- **クリップボードのガイド設定を貼り付け**

### ルックルーム / フォーカルポイント（固有プロパティ）

ガイド種別によって、下のプロパティ欄が変わります。

- **フォーカルポイント**: `X（幅比率）`, `Y（高さ比率）`, `半径（対角線比率）`
- **ルックルーム**: `左右（左/右）`

---

## 表示（オーバーレイ・表示モード）

表示は大きく2系統あります。

- **表示モード**（見開き / カット / パネル）: どの単位を中心に表示するか
- **表示・オーバーレイ**: ガイド、描画、矩形などの表示/不透明度

### 表示モード（ツールバー）

上部ツールバーの **「表示:」** で切り替えます。

- **見開き**: 見開き表示（2ページ、または1ページのみ）
- **カット**: 選択カットの全パネルをグリッド表示
- **パネル**: 選択パネルを1枚にフィット

補足:

- ページ描画は **見開き**でのみ有効です。

### 表示・オーバーレイ（表示タブ）

「表示」タブの「表示・オーバーレイ」で、次を調整できます（UI 表記）。

- **コンテ:** 不透明度（参照画像の不透明度）
- **ガイド:** 表示する / 不透明度（全カット共通・アプリ設定）
- **描画:** 表示する / 不透明度  
  - 「描画の不透明度（描画モード中は常に100%表示・終了後に反映）」
- **ページ描画:** 表示する / 不透明度  
  - 「ページ描画の不透明度（ページ描画モード中は100%表示）」
- **矩形:** 表示する / 不透明度  
  - 「抽出矩形・フレーム矩形…の一括表示」
- **フィット余白:** 「画面に合わせる」時の余白（%）
- **パネル上のカット表示:** パネル上のカット番号・パネル番号・底部情報の表示
- **カット／パネルフォーカスでプライマリに合わせて画像を回転**
- **実験: 範囲矩形の偶奇色**

---

## 書き出し（出力フォルダ・単位・XDTS・テンプレート）

書き出しは、主に「出力フォルダ」「書き出し単位」「書き出し対象」「オプション」の4点を決めます。

### 出力フォルダ（必須）

- **出力フォルダは必須**です（未指定だと実行できません）
- **存在するフォルダを指定**してください（存在しないパスは不可）
- 書き出し後にエクスプローラーで開く設定もあります

### 書き出し単位

書き出し単位は **「書き出し単位:」** から選びます（UI 表示名）。

- **全て**
- **ページ**
- **カット**
- **シーン**
- **担当者**
- **優先度**

使い分けの目安:

- **カット**: カット番号・尺（フレーム）を前提に、カット単位で書き出したいとき（基本）
- **ページ**: ページ単位で書き出したいとき（カット管理より先に素材を切り出す用途）
- **シーン / 担当者 / 優先度**: カットにシーン/担当者/優先度の情報を持たせ、分類単位で書き出したいとき
- **全て**: 対象指定をまとめて扱いたいとき（実際の書き出しパスはカット単位相当になります）

### 書き出し対象（「対象」タブ）

書き出し単位を変えると、対象の選び方も変わります。

- **ページ**: 「書き出すページを選択してください（複数選択可能）」の一覧で選択  
  - ダブルクリックで該当ページへ移動できます
- **カット**: 「書き出すカットを選択してください（複数選択可能）」の一覧で選択  
  - **兼用カット**はまとめてグルーピングされた表示になります（区切り文字は設定の影響を受けます）
  - ダブルクリックで該当カットへ移動できます

（シーン/担当者/優先度/全ての対象 UI は追記予定）

- **シーン**: 「書き出すシーンを選択してください（複数選択可能）」の一覧で選択
  - 事前にカットにシーン割当が必要です（カットテーブル右クリック → **「🎬 シーン管理」**）
- **担当者**: 「書き出す担当者を選択してください（複数選択可能）」の一覧で選択
  - 事前にカットに担当者割当が必要です（カットテーブル右クリック → **「👤 担当者管理」**）
- **優先度**: 「書き出す優先度を選択してください（複数選択可能）」の一覧で選択
  - 事前にカットに優先度割当が必要です（カットテーブル右クリック → **「🔢 優先度管理」**）
- **全て**: 「全ての抽出対象を書き出し」  
  - 現在のグループの **全ページ・全パネル**が対象になり、個別の対象選択は不要です

### よく使うオプション

- **余白を追加**: レイアウト用紙設定の余白を抽出画像に追加
- **書き出し用フォルダをクリアする**: 書き出し用フォルダの内容を削除してから実行
- **書き出し後にフォルダを開く**: 完了後に出力フォルダを開く
- **XDTSファイルを書き出す**: カットごとに XDTS タイムラインファイルを生成
- **テンプレートファイルを書き出す**: 各カットフォルダへテンプレートをコピー（`{cut_identifier}` の置換あり）

---

## ショートカット（確認・編集・初期化）

### ショートカット編集ダイアログ

- メニュー **「編集(&E)」→「ショートカット編集(&K)...」** から開きます。
- ここで各操作のキー割り当てを変更できます。

操作の要点:

- **ツリーをダブルクリック**して編集対象を選びます
- 「新しいキー」欄をクリックして、割り当てたいキーを押します
- **「キーを追加」**で複数キー割り当ても可能です
- **「すべてクリア（無効化）」**で、その操作のショートカットを無効にできます
- **「競合を確認」**で重複（同じキーに複数操作が割り当て）を検出できます
- **「既定に戻す」**で初期状態に戻せます

### 代表的な初期ショートカット

- **元に戻す**: `Ctrl+Z`
- **やり直す**: `Ctrl+Y` / `Ctrl+Shift+Z`
- **カット番号で移動**: `Ctrl+G`

### 設定ファイル（上級者向け）

ショートカットは `config/shortcuts.json` に保存されます（存在しない場合は作成されます）。  
「既定に戻す」は、このユーザー設定をクリアしてデフォルトへ戻す操作に相当します。

（初期化方法・競合の考え方は追記予定）

---

## メニュー別リファレンス

### ファイル(&F)

- **新規プロジェクト(&N)**: 新しいプロジェクトを作成
- **プロジェクトを開く(&O)...**: 既存プロジェクトを開く
- **パネルデータをインポート(&I)...**: パネルデータを読み込み、新規グループを作成
- **保存(&S)** / **名前を付けて保存(&A)...**: プロジェクトを保存
- **終了(&X)**: アプリケーションを終了

### 編集(&E)

- **元に戻す(&U)** / **やり直す(&R)**: 操作の取り消し/やり直し
- **カット番号で移動(&J)...**: カット番号を入力して目的のカットへ移動
- **ショートカット編集(&K)...**: キーボードショートカットを編集
- **環境設定(&G)...**: 環境設定ダイアログを開く（表示言語、ガイド色、起動時のライセンス案内など）。ショートカット `Ctrl+,`（OS により異なる場合あり）

### ライセンス(&L)

未認証時は無料枠（コンテグループ数・カット数など）の制限があります。認証後は制限が解除されます。

- **ライセンス案内を表示(&I)...**: 無料枠の上限と認証・購入の案内
- **ライセンス認証(&A)...**: ライセンスキーを入力して認証
- **All-Access を購入(&S)...**: 全アプリ対応のサブスクリプション（All-Access）の購入ページをブラウザで開く
- **EconTemple 買い切りを購入(&B)...**: EconTemple 単体の買い切りライセンスの購入ページをブラウザで開く
- **ライセンス認証を解除(&U)**: この端末に保存されているライセンス情報を削除

**ライセンスの種類（要点）**

| 種類 | 有効範囲 |
|------|----------|
| **All-Access（サブスク）** | 対象期間中、提供中の全有料アプリで利用可 |
| **EconTemple 買い切り** | EconTemple のみ（永久） |

All-Access 用のキーは EconTemple でも使えます。他アプリ専用の買い切りキーは EconTemple では認証できません。

ステータスバー右側に **「ライセンス: 認証済み」** / **「ライセンス: 未認証」** が表示されます（認証済みのときは All-Access / 買い切りなどの種別も表示されます）。

### ヘルプ(&H)

- **バージョン情報(&A)...**: バージョン情報を表示

---

## よくあるつまずき（原因 → 確認 → 解決）

### 「抽出/書き出し」が押せない

**原因候補**

- 出力フォルダが未指定
- 出力フォルダが存在しない
- 書き出し対象（ページ/カット等）が0

**確認**

- 出力フォルダが指定されているか
- 指定したフォルダが実在するか（エクスプローラーで開けるか）
- 何を書き出す設定になっているか（対象が選ばれているか）

**解決**

- 存在する出力フォルダを指定し直す
- 書き出し対象を追加/選択してから実行する

補足:

- 押せないときは、抽出ボタンのツールチップに理由が出ます（例: 「出力フォルダが存在しません。」「書き出し対象（カット・ページ・パネル）を追加してください。」）。

### 「ページ描画」が使えない（押せない/警告が出る）

**原因候補**

- 見開き表示ではない（ページ画像が表示されていない）
- パネル描画が ON になっている（同時に ON にできない）
- フレーム矩形編集モード中

**確認**

- ツールバーの表示モードが **「見開き」** になっているか
- ツールバーの **「描画:」** が「パネル描画」になっていないか
- 「フレーム矩形を編集（100Fr）」が ON になっていないか

**解決**

- 表示を **「見開き」** に切り替える
- 描画をいったん **「描画オフ」** にしてから **「ページ描画」** を ON にする
- フレーム矩形編集を OFF にする

### ガイドが表示されない

**原因候補**

- 表示タブでガイド表示が OFF
- 不透明度が 0% に近い
- ガイド自体が有効化（チェック）されていない

**確認**

- 表示 → 表示・オーバーレイ → **「ガイド: 表示する」** が ON か
- ガイドの不透明度が極端に低くないか
- カットタブ → 「ガイド」 → ガイド一覧にチェックが入っているか

**解決**

- ガイド表示を ON にし、不透明度を上げる
- 必要なガイド種別をチェックして有効化する

### パネルが検出されない（または一部だけおかしい）

**原因候補**

- 画像のコントラストが低い/解像度が極端
- ページごとに枠線や余白の条件が違う

**確認**

- 別ページでは正常か
- 該当ページだけ条件が違わないか（余白、枠の太さなど）

**解決**

- 該当ページは手動で矩形を調整する
- うまくいく設定があれば、そのページ/グループに合わせて調整する

### XDTS が開けない / 期待通りに読み込めない

**確認**

- CLIP STUDIO PAINT のバージョン（古い場合は更新）
- 出力物が最後まで書き出されているか（途中で失敗していないか）

**解決**

- まずは小さいプロジェクトで XDTS を試し、再現条件を絞る
- 改善しない場合は、再現手順と環境情報を添えて問い合わせる（後述）

---

## 困ったとき（報告・問い合わせ）

不具合報告・質問は [SUPPORT](./SUPPORT.md) を参照してください。
報告の際は、可能な範囲で次を添えると調査が早くなります。

- **現象**（何が起きたか）
- **再現手順**（操作順）
- **環境**（Windows 10/11、64bit）
- **対象ファイルの目安**（PDF/画像、ページ数やサイズ感）

---

## 関連

- [QUICKSTART](./QUICKSTART.md)
- [SUPPORT](./SUPPORT.md) — 不具合報告・質問窓口