# Changelog - EconTemple

このアプリのバージョンごとの変更内容です。

---

## [0.10.0-beta](https://github.com/animtools/EconTemple/releases/tag/v0.10.0-beta) - 2026-08-30

前回（0.9.0-beta）から約3か月分の更新です。書き出しとカット一覧が作り替わり、
配布フォルダ経由でプロジェクトを配る／受け取る経路が入りました。

### 追加

- **配布フォルダ経由の版管理**：版の公開（変更のあったグループだけ積む）・購読側の取り込み
  （欲しいグループだけ選ぶ）・更新の検知とバッジ・差分プレビューと適用・版戻し。
  適用時にローカルの作業（メモ・タグ・描画）は持ち越され、行き場を失ったデータは
  黙って捨てずに退避してから自動で戻ります。
- **カスタム列**：カット一覧に任意の列（テキスト／選択／チェック／数値）を足し、
  絞り込みと書き出しのまとめ方に使えます。
- **カット一覧の多段表示**：ウィンドウ幅に応じて 2〜4 段。段をまたいだ選択、
  文字と行高の密度調整に対応しました。
- **ページ変形**：ページ単位の非破壊の相似変形と、ページ揃えの自動推定。
- **カット結合**：カットブラウザで複数選択して、ページをまたぐカットを1本にまとめられます。
- **コンテ用紙のプレビューサムネイル**。

### 変更

- **書き出しの導線をカットブラウザへ一本化**しました（専用タブは廃止）。
  書き出す対象は「いま選んでいるカット」で、全部出すときは Ctrl+A です。
  実行前の確認ダイアログは無くしました。
- **書き出しのまとめ方を一般化**（なし／シーン／担当者／任意の選択列）。
- ライセンス認証が**スイート共通**になりました。他の Shinosuke ツールと同じ PC で
  使う場合、**同じキーで認証しても端末の枠は1つしか消費しません**。
- **購入導線を追加**しました（これまでボタンが押せない状態でした）。
  All-Access のお申し込みページへ進めます。買い切りプランは準備中です。

### 修正

- ページタブが直前のページを表示するずれ、描画のダブルクリックで2ストローク目が落ちる問題、
  Space でのパン中に描画・選択が誤って動く問題、対象一覧が空選択のときに固まる問題。
- 英語表示のときに一部が日本語のまま出ていた箇所。
- 問い合わせ窓口のアドレスを `shinosuke.support@gmail.com` に統一しました。

---

## [0.9.0-beta](https://github.com/animtools/EconTemple/releases/tag/v0.9.0-beta) - 2026-05-22

Windows 64bit ポータブル版（zip 解凍して `EconTemple.exe` を起動）のベータ配布です。

### 追加

- コンテ画像（PDF／フォルダ）の読み込みとパネル一括切り出し
- バッチ処理・ページ単位のカスタム抽出
- ページ上の描画・注記（作打ちメモ）
- カット管理（自動／手動の割り当て、番号の再計算、兼用カット）
- XDTS 出力（CLIP STUDIO PAINT 用）
- プロジェクトの保存・復元（`.eco`）
- グループ管理
- ライセンス認証（未認証時はコンテグループ数・カット数に制限あり）
- 配布 ZIP 同梱のサンプル（`sample_files/`）

### 変更

- カット一覧の「カットリセット」確認ダイアログで、操作内容が分かるボタンラベルに変更（Yes / No / Cancel の表記を廃止）

### ドキュメント

- 利用者向け README / QuickStart / SUPPORT の表現を整理

---

※ 表示バージョンの正はアプリ内「バージョン情報」です（`0.9.0-beta` など）。
